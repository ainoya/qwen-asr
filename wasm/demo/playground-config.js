/* Deployment configuration for the demo. The local dev server needs nothing
 * here; a hosted playground overwrites this file to point the demo at its
 * model and samples, e.g.:
 *
 *   window.QWEN_PLAYGROUND = {
 *     modelBase: "https://huggingface.co/<user>/<repo>/resolve/main",
 *     modelSize: 2179070272,          // used if HEAD reports no length
 *     sampleEn: "samples/jfk.wav",
 *     sampleJa: "samples/daily__kyoko_normal.wav",
 *   };
 */
